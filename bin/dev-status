#!/bin/zsh
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
APPS=(ballz ballz-royale crapuler games stellar-siege)

for app in "${APPS[@]}"; do
  "$ROOT_DIR/apps/$app/bin/dev-status"
done
